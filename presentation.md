# FreshGuard ver3 — 발표 자료 설명서

> PPT 제작 시 이 문서의 각 슬라이드 항목을 순서대로 사용하세요.
> 각 항목에는 슬라이드 제목, 핵심 메시지, 세부 내용, 발표 멘트 예시가 포함되어 있습니다.

---

## 슬라이드 1. 표지

**제목**: FreshGuard — AI 냉장고 식재료 관리 서비스

**부제**: 사진 한 장으로 식재료를 등록하고, 유통기한 임박 재료로 레시피를 추천받는 AI 기반 식품 관리 플랫폼

**발표자**: [이름]

**날짜**: 2026년 5월

---

## 슬라이드 2. 문제 정의

**제목**: 우리는 왜 음식을 버릴까?

**핵심 메시지**: 냉장고 속 식재료를 파악하지 못해 버려지는 음식이 늘어나고 있다.

**세부 내용**:
- 국내 연간 음식물 쓰레기 발생량: 약 500만 톤 (환경부, 2023)
- 음식물 쓰레기의 주요 원인: 유통기한 초과, 중복 구매, 냉장고 속 재료 파악 실패
- 음식물 처리 비용: 종량제 기준 평균 600원/kg
- 버려지는 식재료의 경제적 가치: 평균 6,000원/kg
- 냉장고 안에 무엇이 있는지 정확히 아는 사람은 드물다

**발표 멘트 예시**:
"냉장고를 열면 언제 샀는지 모르는 재료들이 가득합니다. 파악하지 못한 채 방치되다 버려지는 음식은 환경 문제이자 가계 낭비입니다."

---

## 슬라이드 3. 기존 해결책의 한계

**제목**: 기존 앱들이 실패하는 이유

**핵심 메시지**: 수동 입력의 번거로움이 지속 사용을 막는다.

**세부 내용**:
- 기존 냉장고 관리 앱: 재료를 하나하나 손으로 입력해야 함
- 입력 자체가 귀찮아 이틀 후 앱을 삭제
- 바코드 스캔 방식: 가공식품에만 한정, 신선식품 적용 불가
- 유통기한을 직접 조회·입력해야 하는 불편함

---

## 슬라이드 4. 솔루션 — FreshGuard

**제목**: 사진 한 장으로 해결한다

**핵심 메시지**: AI가 사진을 분석해 식재료를 자동 인식하고, 유통기한 추정부터 레시피 추천까지 처리한다.

**세부 내용**:
- 사진 촬영 또는 업로드 → AI가 식재료 자동 인식
- 로컬 DB 기반 유통기한 즉시 적용 (67종 식품 데이터)
- DB에 없는 식재료는 LLM이 사전 지식으로 유통기한 추정
- 유통기한 D-3 임박 알림
- 임박 재료 기반 AI 레시피 5종 추천
- 소비/폐기 기록으로 환경 기여도 및 절약 금액 계산

---

## 슬라이드 5. 시스템 아키텍처

**제목**: 원격 GPU 서버 기반 분산 아키텍처

**핵심 메시지**: 모바일 브라우저부터 GPU 추론 서버까지 3단 구조로 구성된다.

**아키텍처 다이어그램 (텍스트)**:
```
[모바일 / 브라우저]
       |  HTTP (ngrok or 로컬 네트워크)
       v
[Next.js 웹 서버 — Windows PC, Port 3000]
  - 이미지 전처리 (EXIF 보정, JPEG 압축)
  - API 라우팅 및 인증 토큰 전달
  - 로컬 JSON DB (재고, 소비 로그, 가계부)
       |  HTTP + Bearer Token + IP 화이트리스트
       v
[FastAPI VLM 서버 — Docker Container, Port 8000]
  - /v1/recognize   : 이미지 → 식재료 목록
  - /v1/expiry/suggest : 식재료명 → 유통기한 추정
  - /v1/recipes     : 재료 목록 → 레시피 5종
  - Rate Limiting, 인증, IP 필터링
       |  HTTP
       v
[Ollama — Docker Container, Port 11434]
  - qwen3.5:9b (비전 + 텍스트 통합 모델)
       |
       v
[NVIDIA A100 80GB PCIe — GPU 서버]
```

**주요 설계 결정**:
- VLM 서버를 별도 머신에 분리 → 모바일 기기 사양에 무관하게 대형 모델 사용 가능
- Next.js가 중간 계층으로 토큰 관리 → 클라이언트에 VLM 서버 주소 노출 없음
- 스트리밍 응답 (chunked transfer) → NAT/라우터 유휴 연결 타임아웃 방지

---

## 슬라이드 6. 주요 기능 (1) — 사진 식재료 등록

**제목**: 찍으면 바로 인식

**핵심 메시지**: 카메라 촬영 또는 갤러리 선택 → 자동 인식 → 목록 수정 → 저장

**세부 내용**:
- 모바일: OS 기본 카메라 앱 호출 (`<input capture="environment">`)
- 데스크톱: 웹캠 모달 (getUserMedia API)
- 이미지 전처리: EXIF 회전 보정, 최대 1024px 리사이즈, JPEG 82% 압축 (Sharp)
- AI 인식 결과: 식재료명, 수량, 단위, 신뢰도 표시
- 수량·이름 수정 가능 후 저장
- 저장 시 로컬 DB 우선 조회 → 즉시 유통기한 적용 (VLM 재호출 없음)

---

## 슬라이드 7. 주요 기능 (2) — 직접 입력 및 냉장고 보기

**제목**: 자동 인식이 어려울 때도 간편하게

**핵심 메시지**: 이름만 입력하면 AI가 유통기한·카테고리·보관법을 자동으로 채운다.

**세부 내용**:

직접 입력 기능:
- 식재료명 타이핑 → LLM 기반 유통기한 자동 추정
- 유통기한 직접 수정 가능
- 수량, 단위, 가격 입력 가능

냉장고 보기 (`/fridge`):
- 카테고리별 그룹화 (채소, 육류, 유제품 등)
- 유통기한 남은 일수별 색상 표시 (D-0 빨강, D-3 주황, 정상 초록)
- 소비 완료 / 폐기 처리 버튼
- 부분 소비 지원 (전체가 아닌 일부 차감)
- 카테고리 필터 탭

---

## 슬라이드 8. 주요 기능 (3) — AI 레시피 추천

**제목**: 버려질 재료로 오늘 저녁 메뉴를

**핵심 메시지**: 유통기한 임박 재료를 우선 사용하는 레시피 5종을 AI가 추천한다.

**세부 내용**:
- 임박 재료(D-3 이내) 자동 인식 및 우선 반영
- 필수 사용 재료 직접 선택 가능
- 추천 결과: 요리명, 카테고리, 추천 이유, 사용 재료 목록
- 재료 사용 버튼: 레시피 카드에서 바로 재고 차감 처리
- 외부 링크 연동: 네이버 검색, 만개의레시피, 유튜브

---

## 슬라이드 9. 주요 기능 (4) — 에코 임팩트

**제목**: 잘 먹으면 지구도 지갑도 가벼워진다

**핵심 메시지**: 식재료 소비 기록을 누적해 환경 기여도와 절약 금액을 수치로 보여준다.

**세부 내용**:
- 음식물 절감량 (kg)
- CO2 절감량 (kg) — 식품 카테고리별 탄소 계수 적용
- 순 절약 금액 = 절약 금액 - 폐기 처리 비용
- 최근 7일 소비/폐기 바 차트
- 카테고리별 소비 현황 프로그레스 바
- 가계부: 식재료 구매 지출 기록 및 절약 금액 비교
- 마일스톤 달성 시스템 (8종): 첫 소진, 10개 소진, CO2 2kg 절감, 연속 관리 등

---

## 슬라이드 10. 기술 스택

**제목**: 사용된 기술

| 영역 | 기술 |
|---|---|
| 프론트엔드 | Next.js 14 (App Router), React 18, TypeScript |
| 스타일 | Pure CSS (Tailwind 미사용, 커스텀 디자인 시스템) |
| 백엔드 API | Next.js API Routes (Node.js) |
| VLM 서버 | FastAPI (Python), Uvicorn |
| LLM 엔진 | Ollama |
| AI 모델 | qwen3.5:9b (비전·텍스트 통합), minicpm-v (비전 보조) |
| GPU 인프라 | NVIDIA A100 80GB PCIe |
| 컨테이너화 | Docker Compose |
| 이미지 처리 | Sharp (Node.js) |
| 보안 | Bearer Token 인증, IP 화이트리스트, Rate Limiting (SlowAPI) |
| 데이터 저장 | JSON 파일 기반 (inventory.json, consume_log.json, budget.json) |
| 식품 DB | CSV 기반 로컬 유통기한 데이터베이스 (67종) |
| 외부 접속 | ngrok (개발·시연) / Tailscale (운영) |

---

## 슬라이드 11. 데이터 흐름 — 사진 등록 시나리오

**제목**: 사진 등록의 전체 흐름

**단계별 설명**:
```
1. 사용자가 냉장고 사진 촬영
2. Next.js: EXIF 보정 + 1024px 리사이즈 + JPEG 압축 (Sharp)
3. VLM 서버 /v1/recognize 호출 (multipart)
4. Ollama: qwen3.5:9b로 이미지 분석 → JSON 반환
   {"items": [{"label":"양파","quantity":3,"unit":"개","confidence":0.92}]}
5. Next.js: 로컬 DB 매칭 → 한국어 정규화 → 중복 합산
6. 사용자: 결과 확인, 수량/이름 수정
7. 저장 버튼 클릭:
   - 로컬 유통기한 DB 조회 (67종, 즉시)
   - DB 미등록 식품만 LLM에 유통기한 추정 요청
8. inventory.json에 저장 완료
```

---

## 슬라이드 12. 보안 설계

**제목**: 다층 보안 구조

**핵심 메시지**: VLM 서버는 인증된 Next.js 서버에서만 호출 가능하다.

**세부 내용**:
```
모바일 사용자
    ↓ (VLM 서버 주소 노출 없음)
Next.js 서버
    ↓ IP 화이트리스트 + Bearer Token
VLM 서버 (FastAPI)
    ↓ Rate Limiting (recognize: 10회/분, health: 30회/분)
Ollama
```

- IP 화이트리스트: VLM 서버가 허가된 IP에서 온 요청만 처리
- Bearer Token: Next.js ↔ VLM 서버 간 인증 (모바일은 토큰 불필요)
- Rate Limiting: API 남용 방지
- 이미지 크기 제한: 최대 10MB

---

## 슬라이드 13. 기술적 도전과 해결

**제목**: 개발 과정에서 해결한 주요 문제들

| 문제 | 원인 | 해결 |
|---|---|---|
| fetch failed (사진 인식) | NAT/라우터가 5분 후 유휴 TCP 연결 강제 종료 | FastAPI StreamingResponse로 5초마다 keepalive `\n` 전송 |
| 레시피 빈 응답 | qwen3 계열의 thinking 모드가 토큰을 소모 후 실제 응답 없음 | `think=False` 파라미터로 thinking 모드 비활성화 |
| 저장 시 재추론 | `addItem()`이 매 저장마다 LLM 유통기한 추정 호출 | 로컬 DB 우선 조회, DB 미등록 식품만 LLM 호출 |
| 모델 128k 컨텍스트 | A100에서 70GB 점유, 추론 속도 저하 | `num_ctx: 4096~8192`로 제한 |
| LLM JSON 파싱 실패 | 모델이 단위 포함 숫자(`300g`), 키 이름 오류, 잘린 응답 반환 | 전처리 regex, 잘린 JSON 복구, 대체 키 폴백 |

---

## 슬라이드 14. 시연 시나리오

**제목**: 라이브 데모 순서

**시나리오**:
1. 모바일에서 ngrok URL 접속 → 홈 화면 확인
2. 카메라로 냉장고 내부 촬영 → 식재료 자동 인식 결과 확인
3. 인식된 식재료 수량 수정 → 저장 버튼 클릭 → 재고 즉시 반영
4. 냉장고 보기(`/fridge`) → 카테고리별 재고, 유통기한 D-day 표시 확인
5. 레시피 추천(`/recipes`) → 임박 재료 선택 → 추천 5종 확인 → 재료 사용 처리
6. 에코 임팩트(`/eco`) → CO2 절감량, 절약 금액, 마일스톤 확인

---

## 슬라이드 15. 프로젝트 구조

**제목**: 코드 구조

```
freshguard-ver3/
├── app/                      # Next.js App Router 페이지
│   ├── page.tsx              # 홈 (사진 등록, 임박 알림)
│   ├── fridge/page.tsx       # 냉장고 보기 (카테고리별 재고)
│   ├── recipes/page.tsx      # AI 레시피 추천
│   ├── eco/page.tsx          # 에코 임팩트 대시보드
│   ├── inventory/page.tsx    # 전체 재고 관리
│   └── api/                  # Next.js API 라우트
│       ├── recognize/        # 이미지 인식
│       ├── inventory/        # 재고 CRUD
│       ├── recipes/          # 레시피 추천
│       ├── expiring/         # 임박 재료 조회
│       ├── eco/              # 환경 통계
│       └── budget/           # 가계부
├── lib/                      # 비즈니스 로직
│   ├── vlm.ts                # 이미지 전처리 + VLM 래퍼
│   ├── vlmServer.ts          # VLM 서버 클라이언트
│   ├── inventory.ts          # 재고 관리 (CRUD, 에코 계산)
│   ├── expiryDb.ts           # 로컬 유통기한 DB
│   └── expirySuggest.ts      # LLM 유통기한 추정
└── server/                   # VLM 서버 (별도 머신에서 실행)
    ├── main.py               # FastAPI 앱 (recognize, recipes, expiry)
    ├── startup.py            # Ollama 준비 및 모델 pull
    ├── Dockerfile
    └── docker-compose.yml    # Ollama + FastAPI 컨테이너 구성
```

---

## 슬라이드 16. 향후 발전 방향

**제목**: 다음 단계

**단기 과제**:
- 영수증 OCR 기반 자동 등록 (마트 구매 내역 → 재고 일괄 추가)
- 알레르기 정보 입력 → 레시피 추천 제외 처리
- 푸시 알림: 유통기한 D-1 임박 시 스마트폰 알림

**중기 과제**:
- 다중 사용자 / 가족 냉장고 공유 기능
- 클라우드 배포 (Vercel + 원격 GPU API) → 설치 없이 사용 가능
- 식재료 구매 패턴 분석 → 쇼핑 리스트 자동 생성

**장기 과제**:
- IoT 연동: 스마트 냉장고 센서 → 자동 재고 감지
- 식단 영양 분석 및 균형 잡힌 레시피 추천

---

## 슬라이드 17. 마무리

**제목**: FreshGuard가 만드는 변화

**핵심 메시지**: 사진 한 장이 음식물 낭비를 줄이고, 환경과 가계를 동시에 지킨다.

**정리 문구**:
- 사진 → 자동 인식 → 유통기한 관리 → 레시피 추천 → 에코 임팩트 측정
- AI가 식재료 관리의 번거로움을 제거
- 개인의 소비 습관 변화가 누적되면 환경적 가치로 이어진다

**감사 인사 및 질의응답**

---

## 부록 — 슬라이드 제작 가이드

### 추천 슬라이드 수 및 시간 배분 (15분 발표 기준)

| 섹션 | 슬라이드 | 시간 |
|---|---|---|
| 표지 + 문제 정의 | 1–3 | 2분 |
| 솔루션 + 아키텍처 | 4–5 | 3분 |
| 주요 기능 | 6–9 | 4분 |
| 기술 스택 + 도전과 해결 | 10–13 | 3분 |
| 데모 + 마무리 | 14–17 | 3분 |

### 강조할 차별점

1. **원격 GPU 서버 분리 아키텍처**: 모바일 브라우저에서 A100 GPU 모델을 사용하는 구조
2. **NAT 타임아웃 해결**: 스트리밍 keepalive로 5분 연결 끊김 문제 해결
3. **로컬 DB 우선 조회**: 저장 시 불필요한 LLM 재호출 제거로 UX 개선
4. **qwen3 thinking 모드 제어**: `think=False`로 구조화 JSON 출력 안정화

### 시각 자료 제안

- 슬라이드 5 (아키텍처): 화살표 다이어그램 이미지 삽입
- 슬라이드 11 (데이터 흐름): 순서도 이미지 삽입
- 슬라이드 6~9 (기능): 실제 앱 스크린샷 삽입
- 슬라이드 13 (도전과 해결): 표 형태 유지, 배경색으로 문제/해결 구분
