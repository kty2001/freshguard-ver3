# FreshGuard 사용 메뉴얼

> 냉장고 재료를 사진 한 장으로 등록하고, 임박한 재료로 메뉴를 추천받아 음식물 쓰레기를 줄이는 온디바이스 AI 앱입니다.
> 모든 추론은 로컬 Ollama에서 진행되며, 외부 서버로 전송되는 데이터는 없습니다.

---

## 1. 시작하기

### 1.1 사전 준비

| 항목 | 버전/사양 | 비고 |
|---|---|---|
| Node.js | 18 이상 | `node --version` |
| Ollama | 최신 | https://ollama.com |
| 디스크 | 약 7 GB | 모델 2종 합계 |
| 메모리 | 8 GB 권장 | qwen2.5:7b 추론 시 |

### 1.2 모델 설치

```bash
ollama pull moondream      # 식재료 인식 (VLM, ~1.7 GB)
ollama pull qwen2.5:7b     # 메뉴·유통기한 추정 (Chat, ~4.7 GB)
ollama serve               # 데몬 시작
```

### 1.3 앱 실행

```bash
npm install
cp .env.example .env.local      # 모델명 변경할 때만 수정
npm run dev                     # http://localhost:3000
```

폰에서 같은 Wi-Fi로 접속하면 모바일 앱 UI로 동작합니다.

```bash
ipconfig getifaddr en0          # macOS LAN IP 확인
# 예: http://192.168.0.10:3000
```

> ⚠️ 처음 실행 시 모델 워밍업으로 첫 요청은 10~30초 걸릴 수 있습니다.

---

## 2. 화면 구성

하단 5탭 네비게이션으로 모든 기능에 접근합니다.

| 탭 | 아이콘 | 역할 |
|---|---|---|
| 홈 | 🏠 | 사진 등록, 직접 입력, 임박 재료 알림 |
| 냉장고 | 🧊 | 카테고리별 시각화 |
| 재고 | 📋 | 전체 재고 카드 리스트 (검색·필터) |
| 레시피 | 🍳 | AI 메뉴 추천 |
| 에코 | 🌱 | 누적 임팩트 + 마일스톤 |

상단 햄버거 아이콘에서 **설정** 화면에 진입할 수 있습니다.

---

## 3. 식재료 등록하기

### 3.1 사진으로 등록 (가장 빠른 방법)

1. **홈** 탭에서 `📷 카메라로 찍기` 또는 `🖼️ 갤러리` 선택
2. 미리보기 확인 후 `🪄 AI 인식 시작`
3. Moondream2가 0.5초 안에 식재료 라벨 추출
4. 인식 결과 카드에서:
   - 라벨 텍스트 수정 가능 (예: "사과" → "홍옥")
   - 잘못 인식된 항목은 ✕ 버튼으로 제거
   - `📚 공식 DB` 배지가 붙은 항목은 식약처/USDA 기준 유통기한 적용
   - `DB 미수록` 배지는 기본 7일로 저장됨
5. `저장` 버튼으로 일괄 등록

💡 **팁**
- 한 사진에 너무 많은 재료가 있으면 인식 정확도가 떨어집니다. 5~10개 단위로 찍는 것을 권장.
- 같은 라벨은 중복 저장되지 않습니다 (수량은 무시).
- 이미지는 인식 직후 메모리에서 폐기됩니다. 디스크 저장 X.

### 3.2 직접 입력 (DB에 없는 재료)

1. 홈 탭의 **✍️ 직접 입력** 카드에 식재료 이름 입력 (예: 고등어, 두부, 사과)
2. 0.7초 디바운스 후 자동으로 추천 유통기한 표시
   - `📚 공식 DB`: 67품목 DB에서 매칭됨
   - `🤖 AI 추정`: Qwen2.5가 카테고리/일수 추론
   - `🟡 기본값`: 모델 응답 실패 시 7일 폴백
3. 슬라이더 또는 프리셋(1/3/7/14/30/90일)으로 일수 조정
4. `↺ 추천값으로 되돌리기`로 추천값 복원 가능
5. `＋ 재고에 추가`

---

## 4. 재고 관리하기

### 4.1 냉장고 탭 (🧊)

- **카테고리별 그리드**로 한눈에 파악
- 카테고리는 항목 많은 순으로 정렬
- 각 타일은 D-day 순서로 정렬되며, 임박 항목은 빨간(D-1 이하)/주황(D-3) 테두리
- 타일 탭 → 소진 처리 (`/api/inventory/consume`)

### 4.2 재고 탭 (📋)

전체 활성 재고를 카드 리스트로 표시합니다.

- **검색**: 이름·카테고리에서 부분 매치
- **필터**: 카테고리 단일 선택
- **정렬**: D-day / 이름 / 등록일
- 카드 우측 버튼:
  - `먹었어요`: 소진 처리 → 에코 로그에 기록되고 마일스톤 카운트
  - `🗑️`: 완전 삭제 (에코 통계에 반영되지 않음)

> **소진 vs 삭제**
> - **소진** = 다 먹어서 비웠을 때. CO₂/절감 통계 +1.
> - **삭제** = 잘못 등록했거나 버렸을 때. 통계에 잡히지 않음.

---

## 5. 메뉴 추천받기 (🍳)

1. **레시피** 탭 진입
2. 조건 설정:
   - **인분**: 1~4
   - **최대 조리 시간**: 10~60분
   - **추천 개수**: 3~9개 (기본 6)
3. `✨ 메뉴 추천 받기` 클릭 (14~20초 소요)
4. 각 카드에는:
   - 메뉴 이름 + 카테고리 이모지 (찌개/볶음/구이 등)
   - **사용 재료** (임박한 것 우선)
   - **추천 이유** (예: "고등어 D-1 소진 우선")
   - **외부 검색 단축 버튼**: 네이버 / 만개의레시피 / 유튜브
5. 임박 재료가 0개면 "임박 항목이 없어요. 평소 메뉴 추천"으로 동작

> ⚠️ 조리법 본문은 생성하지 않습니다. 검색 링크로 외부에서 확인하세요.

---

## 6. 에코 임팩트 보기 (🌱)

소진 처리한 항목 기준 누적 임팩트가 자동 집계됩니다.

| 지표 | 산출 기준 |
|---|---|
| 음식물 절감 (kg) | 항목별 평균 중량 합산 |
| CO₂ 절감 (kg) | Poore & Nemecek 2018 식재료별 환산 계수 |
| 절약 금액 (원) | 평균 단가 × 소진 수량 |
| 누적 소진 (건) | consume_log 전체 건수 |
| 연속 관리 (일) | 마지막 소진까지 연속 일수 |

### 6.1 차트

- **최근 7일**: 일별 소진 건수 막대 그래프
- **카테고리별 소진**: 카테고리 색상별 누적 막대
- **마일스톤**: 8종 배지 (예: 첫 소진, 10건 달성, 1kg 절감 등) — 달성 시 초록 ✅

---

## 7. 설정 (⚙️)

### 7.1 AI 모델 확인

`.env.local`에서 모델 변경 가능:

```bash
OLLAMA_VLM_MODEL=moondream
OLLAMA_CHAT_MODEL=qwen2.5:7b   # 대안: llama3.1, gemma2:9b, yi:9b
OLLAMA_HOST=http://localhost:11434
```

변경 후 `npm run dev` 재시작 필요.

### 7.2 데이터 현황

- 유통기한 DB 품목/카테고리 수
- 활성 재고 / 누적 등록 / 소진 이력 건수

### 7.3 데이터 초기화 (⚠️ 복구 불가)

| 버튼 | 삭제 대상 |
|---|---|
| 재고 데이터 삭제 | `data/inventory.json` |
| 에코 데이터 삭제 | `data/consume_log.json` |
| 전체 초기화 | 위 두 파일 모두 |

---

## 8. 자주 묻는 질문

**Q. 인식이 자꾸 "사과" 대신 "apple"로 나와요.**
A. 라벨을 직접 한국어로 수정 후 저장하세요. 후처리 사전(`polishKorean`)이 일부 영문→한국어 변환을 처리하지만, 모든 케이스를 잡진 못합니다.

**Q. DB에 없는 재료(예: 고등어)도 정확하게 추천되나요?**
A. Qwen2.5:7b가 카테고리(`수산`)와 보관 일수(`냉장 2일`)를 추정합니다. 응답이 어색하면 슬라이더로 직접 조정하세요.

**Q. 메뉴 추천이 14초 이상 걸려요.**
A. qwen2.5:7b 평균 응답이 14~20초입니다. 첫 호출은 모델 로딩으로 더 오래 걸릴 수 있습니다. 더 빠른 응답을 원하면 `.env.local`에서 `gemma2:2b` 같은 경량 모델로 교체 가능 (한국어 품질은 다소 떨어짐).

**Q. 내 사진이 어디로 가나요?**
A. 어디에도 저장되지 않습니다. multipart로 받아 Moondream에 전달 후 즉시 폐기. `data/` 폴더는 메타데이터(라벨, 일자)만 보관합니다.

**Q. 같은 재료를 또 찍으면 어떻게 되나요?**
A. 같은 라벨은 한 번만 저장됩니다. 수량을 늘리려면 직접 입력에서 같은 이름으로 다시 등록하세요 (별도 항목으로 추가됨).

**Q. 폰에서 카메라가 안 열려요.**
A. HTTPS가 아닌 LAN IP(`http://`)는 일부 브라우저에서 카메라 권한이 막힐 수 있습니다. Safari/Chrome 모바일 권한 설정을 확인하거나 `localhost` 터널(예: `ngrok`)로 HTTPS 접속을 시도하세요.

**Q. Ollama가 안 떠 있을 때 어떻게 되나요?**
A. `/api/recognize`, `/api/recipes`, `/api/expiry/suggest`가 타임아웃 후 에러를 반환합니다. 직접 입력은 `🟡 기본값` 7일로 폴백되어 등록은 가능합니다.

---

## 9. 트러블슈팅

| 증상 | 원인 / 조치 |
|---|---|
| `Ollama not reachable` | `ollama serve` 실행 여부 확인. 포트 11434 점유 충돌 시 `OLLAMA_HOST` 변경 |
| 인식 결과 0개 | 사진이 어둡거나 재료가 가려진 경우. 밝은 곳에서 다시 시도 |
| 메뉴 추천 빈 응답 | 활성 재고가 0개인 경우. 재고를 먼저 등록 |
| `OOM` 또는 큰 이미지 에러 | sharp가 자동 리사이즈하지만 30MB 초과 시 거부. 폰 카메라 해상도 낮춰 재시도 |
| D-day가 음수 | 이미 만료된 항목. 색상은 빨강, 소진 또는 삭제 처리 |
| 에코 통계 0 | 항목을 **소진** 처리해야 반영. **삭제**는 반영 안 됨 |

---

## 10. API 직접 호출 (개발자용)

| Method | Path | 본문 |
|---|---|---|
| POST | `/api/recognize` | multipart `image` |
| POST | `/api/expiry/suggest` | `{ "name": "고등어" }` |
| GET | `/api/inventory` | — |
| POST | `/api/inventory` | `{ "items": [{"display_name":"사과"}] }` 또는 `{"display_name":"두부","manual":true,"manual_expiry_days":5}` |
| DELETE | `/api/inventory?id=<id>` | — |
| POST | `/api/inventory/consume` | `{ "id": "<id>" }` |
| DELETE | `/api/inventory/all?scope=inventory\|logs\|all` | — |
| GET | `/api/expiring?days=3` | — |
| POST | `/api/recipes` | `{ "servings":2, "max_minutes":20, "threshold":3, "count":6 }` |
| GET | `/api/eco/summary` | — |
| GET | `/api/meta` | — |

---

## 11. 참고

- **데이터 출처**: 식약처 고시 제2022-31호 · USDA FoodKeeper · Poore & Nemecek 2018
- **PRD 원문**: `FreshGuard_PRD_v2.0.docx`
- **테스트 로그**: `test_log.md` (Iteration 1~10)
- **저장소 구조 / 빌드 / 라이선스**: `README.md` 참고
